# 使用公网官方 npm 源。
# 部署平台（如 Vercel）在境外，必须用公网源才能下载依赖。
# 若你在公司内网本地开发遇到装包慢/失败，可临时用内网镜像：
#   npm install --registry=http://bnpm.byted.org
# 但不要把内网地址提交进 package-lock.json（会导致线上部署失败）。
registry=https://registry.npmjs.org/
